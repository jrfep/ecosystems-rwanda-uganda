We will use environment `ee` with the setup for connecting to earth-engine.
We will install packages required to work with iNaturalist API

```sh
conda activate ee
pip install ipyplot pyinaturalist rich
```

```sh
conda activate ee
jupyter lab
```